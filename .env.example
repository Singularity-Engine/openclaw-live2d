# Backend API server URL (for development proxy)
VITE_API_TARGET=http://localhost:12393

# Backend WebSocket URL (for development proxy)
VITE_WS_TARGET=ws://localhost:12393

# Public URLs (for Docker/production builds)
# VITE_API_URL=https://your-domain.com
# VITE_WS_URL=wss://your-domain.com

# Deployment (optional, for deploy.sh)
# DEPLOY_SERVER_IP=your-server-ip
# DEPLOY_SERVER_USER=your-ssh-user
# DEPLOY_SSH_KEY=~/.ssh/id_rsa
